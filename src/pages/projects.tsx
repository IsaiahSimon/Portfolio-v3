import React from "react";
import Layout from "../components/Layout";

const ProjectsPage = () => {
  return (
    <Layout>
      <h1>My Projects</h1>
      <p>
        This page will show a list will list out my projects, possibly fetched
        from a database or a static file.
      </p>
    </Layout>
  );
};

export default ProjectsPage;
